Hermes
======

A minimalist GET/HEAD-only HTTP server for hosting static content.

It does not support TLS: you might want to use a TLS reverse proxy instead.

Build and install
-----------------

	$ make
	# make install

Dependencies
------------

- C compiler (C99)
- libc
- POSIX make (optional)

Documentation
-------------

See the man page of hermes(1).
